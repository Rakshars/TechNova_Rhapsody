services:
  backend:
    build: ./backend
    container_name: technova-backend
    ports:
      - "5000:8000"

  frontend:
    build: ./frontend
    container_name: technova-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend